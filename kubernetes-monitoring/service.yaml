apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: homework
spec:
  selector:
    app.kubernetes.io/instance: nginx
    app.kubernetes.io/name: nginx
  type: ClusterIP
  ports:
  - port: 9113
    protocol: TCP
